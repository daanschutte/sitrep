services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=sitrep'
      - 'POSTGRES_PASSWORD=secret-dev-password'
      - 'POSTGRES_USER=dev'
    ports:
      - '5432:5432'
