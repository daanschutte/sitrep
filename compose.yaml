services:
  postgres:
    image: 'postgres:17'
    environment:
      - POSTGRES_DB
      - POSTGRES_PASSWORD
      - POSTGRES_USER
      - APP_USER_PASSWORD
    ports:
      - '${POSTGRES_PORT}:5432'
    volumes:
      - ./docker/init:/docker-entrypoint-initdb.d
